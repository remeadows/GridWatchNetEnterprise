apiVersion: 1

providers:
  - name: "GridWatch Dashboards"
    orgId: 1
    folder: "GridWatch NetEnterprise"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
